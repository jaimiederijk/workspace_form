import { ListCheckDirective } from './list-check.directive';

describe('ListCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new ListCheckDirective();
    expect(directive).toBeTruthy();
  });
});
